import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import'./style.css';const sb=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY);function App(){const[x,setX]=useState([]);useEffect(()=>{sb.from('site_settings').select('value').eq('key','online_classes').maybeSingle().then(({data})=>{try{setX(data?.value?JSON.parse(data.value).filter(c=>c.published!==false):[])}catch{setX([])}})},[]);return <main style={{maxWidth:1100,margin:'auto',padding:'50px 22px'}}><a href="./">← بازگشت به رازی‌تک</a><h1>🎥 کلاس‌های آنلاین</h1><p>کلاس‌های آنلاین برگزارشده و برنامه‌ریزی‌شده را اینجا ببینید.</p>{!x.length?<div className="empty">هنوز کلاس آنلاینی ثبت نشده است.</div>:x.map(c=><article className="panel" style={{margin:'15px 0'}} key={c.id}><h2>{c.title}</h2><p>{c.description}</p><p>👨‍🏫 {c.teacher}　 📅 {c.date}　 ⏰ {c.time}</p><a className="primary" href={c.meeting_url} target="_blank" rel="noreferrer">ورود به جلسه آنلاین ↗</a></article>)}</main>}createRoot(document.getElementById('classes-root')).render(<App/>);